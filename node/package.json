{
  "name": "@openssl-sys-kotlin/openssl-shim",
  "version": "0.1.0",
  "private": true,
  "description": "Node N-API shim that re-exports a narrow slice of openssl-sys to Kotlin/JS. Build-time companion to openssl-sys-kotlin's jsNodejs target.",
  "main": "index.js",
  "scripts": {
    "build:native": "node-gyp configure build",
    "rebuild": "node-gyp rebuild",
    "clean": "node-gyp clean"
  },
  "engines": {
    "node": ">=22"
  },
  "files": [
    "binding.gyp",
    "src/openssl_shim.cc",
    "index.js"
  ],
  "dependencies": {
    "node-addon-api": "^8.5.0"
  },
  "devDependencies": {
    "node-gyp": "^11.0.0"
  }
}
